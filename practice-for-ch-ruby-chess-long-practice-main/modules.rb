module Slideable

    HORIZONTAL_DIRS =
    DIAGONAL_DIRS

    def moves(directions)
        # returns array of places piece can move to
    end
end

module Stepable
    def moves(directions)
        # returns array of places piece can move to
    end
end
